# Legacy reference files

Preserved from the predecessor `TB-Abandonment-Analysis` repo for historical
reference. **Not part of the active pipeline** — do not run as-is.

- `MASTER_RUN_ANALYSIS.py` — orchestrator for the old (pre-ITT) analysis pipeline
- `config.py` / `config.R` — path configuration pattern (cleaner than the hardcoded
  paths in the root-level `00_`–`04_` scripts; kept in case we refactor later)
- `setup_sherlock_env.sh` — environment setup for Stanford Sherlock cluster runs
- `README_abandonment.md` — initial project README, superseded by top-level `README.md`

The current authoritative pipeline is `ITT_Analysis/`.
